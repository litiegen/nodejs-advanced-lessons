/**
 * npm(node package manager)包管理器
 * 会有大量的第三方上传的模块
 * 
 * 它是一个远端的模块的服务器
 * 
 * npm install 模块名称
 * 存在该模块会下载到当前目录文件夹中
 */
const now = require("date-now");
console.log(Date.now());
console.log(now());